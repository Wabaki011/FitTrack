// config/config.js
module.exports = {
    auth: {
        saltRounds: 10,
    },
    nutrition: {
        // Could add configurable nutrition settings here later
    },
    // Add other configuration sections as needed
};
